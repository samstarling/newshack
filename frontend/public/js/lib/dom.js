define(['jquery'], function ($) {
    var storyline = $('.storyline'),
        app = $('.app'),
        video = $('.video');

    return {
        storyline: storyline,
        app: app,
        video: video
    };
});